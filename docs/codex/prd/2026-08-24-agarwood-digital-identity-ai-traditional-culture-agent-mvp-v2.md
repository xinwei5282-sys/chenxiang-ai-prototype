---
artifact: prd
version: "3.0"
created: 2026-08-24
updated: 2026-08-31
status: review-ready
---

# 琼南沉香小程序 MVP PRD：数字身份 + AI传统文化解读 Agent

- 文档版本：3.0 评审交付版。
- 产品形态：微信小程序；当前 React 项目为需求与交互原型，不代表生产能力已完成。
- 适用对象：产品、设计、小程序前端、后端、AI 工程、测试、内容、运营与法务。
- 真源规则：本文是当前产品行为、页面边界和验收口径的唯一真源；历史 PRD、设计稿和实施计划仅作过程参考。
- 需求用语：`必须`为 P0，`应`为 P1，`可以`为 P2；未决事项不得被研发或编码 Agent 自行补成产品结论。
- 写法参考：杭小消/消保委页面级 PRD，以页面目标、角色权限、业务规则、字段内容、操作结果、异常状态和验收标准组织需求。

## Problem Statement

### 1. 用户问题

购买或收到沉香手串后，用户首先需要确认产品身份、证书、产区和养护方式；完成核验后，还需要一个值得再次打开的品牌服务，用于查看藏品、了解沉香知识和记录个人问帖。

当前需求同时覆盖数字身份、内容服务、AI 对话和个人记录。如果只按页面视觉开发，容易出现手串与证书错配、授权后不恢复原操作、AI 输出越界、历史记录语义混乱，以及原型演示能力被误当成真实线上能力等问题。

### 2. 业务问题

- 单次证书核验缺少复访价值，不能独立支撑长期用户关系。
- 多手串用户会绑定多张证书，个人中心不能继续把“证书数量”直接跳到任意一张证书。
- AI 问帖若被包装成确定性算命或改运工具，会增加平台审核、品牌信誉和用户依赖风险。
- 首页同时承担品牌展示、商品预告、藏品、服务和内容，必须明确主次，避免所有模块等权。
- 商城、真实 NFC、真实微信身份、远程 AI、电子证书和产业园内容均依赖外部系统，需求原型不能用模拟行为替代生产合同。

### 3. Why now

- 首页、问帖、我的、证书、养护、产业园和知识文章的视觉与流程已完成多轮确认，可以进入统一评审。
- 2026-08-28 至 2026-08-31 的客户反馈已显著改变原 2.0 PRD，包括四个根 Tab、滚动首页、多藏品、多证书、纯文字问帖、Profile Banner 和公众号式文章。
- 原 2.0 PRD仍包含语音输入、三 Tab、单证书入口、继续旧对话和传统文化帮助页等过期结论，继续使用会直接造成返工。

### 4. 目标用户

- U1：已购买或收到广垦沉香手串，通过芯片、二维码或微信入口查看产品身份的用户。
- U2：拥有一串或多串已绑定手串，需要查找对应证书和溯源记录的用户。
- U3：希望了解沉香知识、养护方法、产业园和认种服务的普通用户。
- U4：希望以传统文化角度梳理挂心事，并保存问帖记录的微信用户。
- U5：负责品牌内容、证书、产业园资料和 AI 安全审核的运营、客服与内容人员。

### 5. 核心术语

- `当前手串`：本次芯片、二维码或首页上下文明确指向的手串。
- `已绑定手串`：已与当前业务账号建立关系、可从“我的手串”列表访问的手串。
- `数字身份`：手串编号、证书、产区、香韵、签发主体和溯源时间线的组合信息。
- `问帖`：用户与 AI传统文化解读 Agent 的一次独立对话。
- `新对话`：每次从首页、根 Tab 或历史详情进入 AI 问事时重新创建的会话，不自动恢复上次聊天。
- `安全转向`：AI 对高风险或越界请求不作结论，改为说明边界并提供现实求助方向。
- `展示型内容`：只供阅读，不提供点赞、评论、分享、收藏、输入或发布操作的内容页面。

## Solution

### 1. 产品定位

产品定位为 `沉香数字身份 + AI传统文化解读 Agent + 广垦沉香内容服务`。

- 信任价值：通过数字身份、证书、溯源、藏品档案和产业园资料说明产品来处。
- 使用价值：通过佩戴养护、沉香知识和公众号式文章提供可持续阅读内容。
- 复访价值：通过 AI 问帖和问帖记录形成个人使用轨迹。
- 品牌价值：通过广垦沉香甄选、认种与产业园内容承接品牌与后续商业化，但 MVP 不建设商城交易。

### 2. 全局产品规则

- 根导航固定为 `首页 / 商城 / 问帖 / 我的`。
- 商城仅显示“即将上线”轻提示，不进入空白页或伪商城。
- 首页是滚动内容页，模块顺序固定，不要求一屏展示全部内容。
- 首页当前手串上下文明确，因此“证书与溯源”直接进入当前手串详情。
- “我的”中的证书入口表达账号拥有的全部手串，必须先进入“我的手串”列表，再进入所选证书。
- 每次进入 AI 问事均开始新对话；历史详情只读，点击后续行动也开启新问帖，不续接旧会话。
- AI 页面只支持文字输入与发送，不支持图片、文件、相机、附件、语音或录音。
- AI 不使用手串编号、材质、产区、年份、证书或香韵推断命运。
- 所有 AI 页面持续展示 `内容由 AI 生成，仅供娱乐参考`。
- Profile 下方只保留 `芯片识别说明`，不展示 `关于传统文化解读`。
- 养护手册与“沉香小知识”文章是两套入口：前者为七则养护指南，后者为公众号式纯阅读文章。
- 证书、档案、产业园、知识文章中的正式事实必须来自可核验数据或已审核内容，客户端和 AI 不得补写缺失事实。
- 原型中的数量、证书、PDF、授权、视频和 AI 回复均为演示数据；生产环境必须接真实接口并呈现真实状态。

### 3. 成功指标

- 身份解析成功率：有效芯片或二维码请求中成功返回当前手串身份的比例，目标不低于 99.5%。
- 证书匹配准确率：从“我的手串”选择后打开对应证书且编号一致的比例，目标 100%。
- 问帖有效完成率：开始新问帖后获得至少一次追问及完整结构化参考的会话比例，目标不低于 70%。
- 授权恢复率：因问帖或记录触发授权后，成功返回原意图的比例，目标不低于 98%。
- 内容到达率：养护、知识文章和产业园入口点击后成功展示正文的比例，目标不低于 99%。
- AI 高风险漏放率：安全评测集中应转向却正常回答的比例，发布门槛为 0 个严重样本。
- 正式上线前由数据负责人确认事件定义、去重主键、统计周期和基线；原型演示数据不进入指标。

### 4. 信息架构

- `首页`：品牌主视觉、AI 主入口、广垦甄选、我的藏品、四项服务、产业园、沉香小知识和版权信息。
- `商城`：仅“即将上线”反馈，不承载商品、购物车、订单或支付。
- `问帖`：进入授权判断后打开新的 AI 对话。
- `我的`：微信账号授权、问帖记录、证书数量和芯片说明。
- `页面 PRD`：仅存在于 React 原型手机模型外；入口为浏览器右侧中部的竖向按钮，打开与消保委参考一致的遮罩和全高字段级抽屉，不属于生产小程序信息架构。

### 5. 核心流程

#### FLOW-01 芯片识别到首页

1. 用户通过 NFC、芯片链接或二维码携带 `scan_token` 打开小程序。
2. 客户端请求手串解析接口，服务端校验凭证、手串和证书状态。
3. 成功后进入 P-01 并建立当前手串上下文；失败进入 P-00，不展示演示身份。
4. 用户从首页查看证书时直接进入当前手串 P-07。

#### FLOW-02 微信授权与意图恢复

1. 用户点击需要账号的问帖或问帖记录。
2. 未授权时打开 P-02 手机内授权层，并保存 `pending_intent`。
3. 用户确认后完成微信业务会话；成功自动执行原意图，失败保留原页面和重试入口。
4. 用户主动点击 Profile 的 `微信授权登录` 时，只切换账号状态，不自动进入其他页面。

#### FLOW-03 新问帖与主动追问

1. 每次进入 P-03 都展示新的空态、广垦沉香 Logo、三个快捷主题和文字输入框。
2. 用户点击快捷主题或输入自由问题后，空态切换为连续消息流。
3. Agent 先追问与当前问题相关的背景点，再在信息充分时输出 `判断 / 提醒 / 建议`。
4. 页面内允许继续对话；离开后再次进入时重新开始新会话。

#### FLOW-04 历史记录与开启新问帖

1. 用户从 `我的 → 问帖记录` 进入 P-05。
2. 点击记录进入 P-06，只读展示历史消息。
3. 点击 `开启新问帖` 创建新 conversation，不向原 conversation 追加消息。

#### FLOW-05 多手串证书

1. Profile 展示 `3 串 / 证书数量` 演示值。
2. 点击进入 P-12 `我的手串`，内容区直接展示手串列表，不展示眉题、大标题或说明。
3. 点击任一手串进入 P-07，对应名称、编号、产区、香韵和时间线随所选记录变化。
4. 从详情返回 P-12，并保留原列表位置。

#### FLOW-06 藏品档案与电子证书

1. 首页“我的藏品”在手串与认种沉香树之间切换。
2. 用户可查看对应 P-13/P-14 档案，也可下载对应演示电子证书。
3. 下载失败时保留页面并提示重试；认种档案的“前往认种”只提示商城即将上线。

#### FLOW-07 内容阅读

1. 首页四项服务分别进入当前手串证书、七则养护、沉香知识和认种档案。
2. 产业园 Banner 进入 P-15 图集与视频展示。
3. 首页两条“沉香小知识”进入 P-16 公众号式纯展示文章。
4. `沉香知识` 中的唯一农垦故事入口进入 P-10。

### 6. 页面清单

- P-00 识别结果页：无 token、解析中、无效 token、撤销和网络失败。
- P-01 首页：完整品牌内容与服务入口。
- P-02 登录确认层：微信授权、取消和意图恢复。
- P-03 AI问事：新对话空态、连续文字消息、思考与安全转向。
- P-04 我的：账号 Banner、问帖记录、证书数量和芯片说明。
- P-05 问帖记录：历史列表、空态和分页。
- P-06 问帖详情：只读历史与开启新问帖。
- P-07 证书与溯源：所选手串的数字身份和时间线。
- P-08 佩戴养护：七则编辑式养护指南。
- P-09 沉香知识：形成、闻香、产区和农垦故事入口。
- P-10 农垦故事：产区、农垦与从林场到手串。
- P-11 芯片识别说明：三步识别和失败重试。
- P-12 我的手串：账号绑定手串列表。
- P-13 手串档案：藏品详情与电子证书。
- P-14 认种档案：认种树信息、生长时间线和证书。
- P-15 源头产业园：四图图集与介绍视频。
- P-16 沉香小知识文章：两篇公众号式纯展示文章。
- P-17 商城占位：统一的“即将上线”轻提示。

### 7. 页面规格

#### P-00 识别结果页

- 页面目标：无法建立当前手串上下文时给出明确原因和恢复路径。
- 适用角色：匿名用户和已登录用户。
- 页面入口：无 token、解析中、无效 token、撤销证书或网络失败。
- 业务规则：失败状态不得展示演示手串、已核验标签或旧缓存证书。
- 字段与内容：状态标题、说明、request_id、重试、识别说明和人工帮助。
- 操作与结果：重新识别再次解析；查看说明进入 P-11；人工帮助打开已配置客服方式。
- 异常与空状态：网络失败保留重试；客服未配置时只显示 request_id。
- 页面验收：所有失败态可恢复且不会误展示成功身份。

#### P-01 首页

- 页面目标：以广垦沉香品牌内容建立信任，并让用户进入问帖、藏品、证书、养护、知识、认种和产业园。
- 适用角色：匿名用户和已登录用户。
- 页面入口：芯片识别成功、根 Tab `首页` 或小程序常规入口。
- 业务规则：按 `品牌主视觉 → AI 问帖 → 广垦沉香甄选 → 我的藏品 → 四项服务 → 产业园 → 沉香小知识 → 版权` 排列并纵向滚动。
- 字段与内容：品牌与产品信息、AI 主入口、四个甄选项、两件藏品、四项服务、产业园摘要、两篇文章和公司版权。
- 操作与结果：主视觉和证书服务直达当前手串 P-07；问帖进入 P-02/P-03；甄选和商城只提示即将上线；藏品可打开档案或下载证书。
- 异常与空状态：主视觉失败显示文本；单个内容模块失败不阻塞其他模块；下载失败提示重试。
- 页面验收：模块顺序和入口语义一致；双设备无横向溢出，固定根 Tab 不遮挡末尾版权。

#### P-02 登录确认层

- 页面目标：解释微信授权用途，并在确认后恢复原操作。
- 适用角色：未建立业务会话的用户。
- 页面入口：开始问帖、根问帖 Tab、问帖记录或 Profile 主动授权。
- 业务规则：问帖和记录必须登录；公开证书、养护、知识和芯片说明不被授权层阻断；授权状态全局共享。
- 字段与内容：标题、用途说明、确认、取消和原型模拟授权提示。
- 操作与结果：确认后建立会话并执行 pending_intent；取消关闭且不创建会话。
- 异常与空状态：授权失败保留来源意图并允许重试，不得循环自动登录。
- 页面验收：授权层位于手机范围内；成功后不要求重复点击来源入口。

#### P-03 AI问事

- 页面目标：用简洁的移动对话完成自由提问、必要追问和传统文化角度参考。
- 适用角色：已完成业务授权的用户。
- 页面入口：首页开始问帖、根 Tab `问帖`、历史详情的 `开启新问帖`。
- 业务规则：每次进入均创建新对话；默认展示 Logo、`今天想问什么？`、说明和 `事业 / 感情 / 财运`。
- 字段与内容：用户消息、Agent 消息、思考状态、文字草稿、免责声明和 `判断 / 提醒 / 建议`。
- 操作与结果：快捷主题直接发送；文字可点击发送或按回车发送；空内容和思考中禁止重复发送。
- 异常与空状态：请求失败保留用户消息与草稿；高风险问题安全转向；长文本自然换行。
- 页面边界：不展示头像、在线状态、独立结果卡、卦象、运势分、手串信息、图片、文件、附件、相机、麦克风或语音。
- 页面验收：新会话、追问、结构化回答、自动滚动、键盘避让和重新进入重置均可操作。

#### P-04 我的

- 页面目标：用重点明确的账号 Banner 承载授权状态和个人数据入口。
- 适用角色：匿名用户和已授权用户。
- 页面入口：根 Tab `我的`。
- 业务规则：Banner 通铺至状态栏并左右贴边；头像、账号文案和授权/登录状态同一横排；下半部只展示问帖记录与证书数量；下方只保留芯片说明。
- 字段与内容：未授权显示 `微信账号 / 授权后同步香事记录 / 微信授权登录`；已授权显示 `微信用户 / 已完成微信授权 / 已登录`；演示数量为 `1 条 / 3 串`。
- 操作与结果：授权按钮打开 P-02；问帖记录未授权时先授权再进入 P-05；证书数量进入 P-12；芯片说明进入 P-11。
- 异常与空状态：授权失败不改变账号状态；数据失败显示 `--`，不伪造数量。
- 页面边界：不展示个人香事档案、记录与管理、使用帮助、重复记录/证书行或传统文化解读入口。
- 页面验收：数量在上、标题在下且居中；统计无卡片底色；双设备在固定根 Tab 上方一屏完整展示。

#### P-05 问帖记录

- 页面目标：让用户查找已保存问帖并进入只读详情。
- 适用角色：已授权用户。
- 页面入口：P-04 `问帖记录`。
- 业务规则：按 updated_at DESC，生产环境每页 20 条并使用 cursor 分页。
- 字段与内容：标题、相对时间和摘要；标题为空时使用首条用户消息摘要。
- 操作与结果：点击记录进入 P-06；空态操作进入新的 P-03。
- 异常与空状态：无记录展示明确空态；失败保留已加载数据并提供重试。
- 页面验收：未授权不请求列表；列表点击不直接路由到新问帖。

#### P-06 问帖详情

- 页面目标：只读查看历史上下文，并允许开启新的问帖。
- 适用角色：记录所有者。
- 页面入口：P-05。
- 业务规则：不提供输入框，不续接原 conversation_id。
- 字段与内容：主题、时间、完整消息时间线、AI 标识和免责声明。
- 操作与结果：`开启新问帖` 创建新 conversation 并进入 P-03。
- 异常与空状态：不存在或越权统一提示“这条问帖记录已不存在”。
- 页面验收：历史内容只读；新问帖进入全新空态。

#### P-07 证书与溯源

- 页面目标：展示当前或所选手串的数字身份、核验状态和时间线。
- 适用角色：持有效扫描会话的匿名用户，或拥有绑定关系的已授权用户。
- 页面入口：首页当前手串直达；P-12 选择后进入；手串档案可进入。
- 业务规则：首页读取 current_bracelet_id；列表读取 selected_bracelet_id；名称、编号、产区、香韵和时间线必须来自同一记录。
- 字段与内容：手串名称、证书编号、核验状态、产区、香韵和时间线。
- 操作与结果：返回原来源；详情页不增加左右滑动、下拉或相邻证书切换。
- 异常与空状态：缺失显示 `信息待补充`；撤销证书不得显示已核验。
- 页面验收：从第二条进入不得显示第一条名称或编号；首页仍直达当前手串。

#### P-08 佩戴养护

- 页面目标：提供完整、连续、适合移动阅读的日常养护指南。
- 适用角色：所有用户，匿名可见。
- 页面入口：首页 `佩戴养护`。
- 业务规则：使用编辑式题图、七则连续正文和底部养护提醒，不采用公众号文章交互。
- 字段与内容：远离化学品、避水与护肤品、避高温与油烟、密封存放、香味恢复、轻柔清洁、日常盘护。
- 操作与结果：仅滚动阅读和返回。
- 异常与空状态：失败优先展示带版本提示的缓存，无缓存时重试。
- 页面验收：七则内容完整、顺序固定且不被安全区遮挡。

#### P-09 沉香知识

- 页面目标：提供沉香基础知识并承接唯一农垦故事入口。
- 适用角色：所有用户，匿名可见。
- 页面入口：首页 `沉香知识` 或知识区 `查看更多`。
- 业务规则：基础知识与首页两篇“沉香小知识”文章是不同内容层级。
- 字段与内容：沉香如何形成、怎样闻香、常见产区差异和 `琼南农垦沉香`。
- 操作与结果：农垦故事进入 P-10。
- 异常与空状态：内容失败使用版本缓存或重试。
- 页面验收：三个主题可读；农垦故事入口唯一且不混入证书时间线。

#### P-10 农垦故事

- 页面目标：解释产区环境、农垦积淀和从林场到手串的过程。
- 适用角色：所有用户，匿名可见。
- 页面入口：P-09 `琼南农垦沉香`。
- 业务规则：品牌与产区内容独立于具体手串证书。
- 字段与内容：产区环境、农垦积淀、从林场到手串和说明提示。
- 操作与结果：只读与返回。
- 异常与空状态：使用版本缓存或重试。
- 页面验收：不出现具体手串证书时间线，不宣传无法举证的权威结论。

#### P-11 芯片识别说明

- 页面目标：说明如何用 NFC 识别手串及失败重试。
- 适用角色：所有用户，匿名可见。
- 页面入口：P-04 唯一帮助行或 P-00 失败态。
- 业务规则：根页面只显示单行标题，完整说明只在二级页展示。
- 字段与内容：轻触芯片、识别成功进入首页、失败调整位置，以及“芯片不会直接开始问帖”。
- 操作与结果：阅读、返回和生产客服入口。
- 异常与空状态：不支持 NFC 时提示二维码或客服替代路径。
- 页面验收：三步说明完整，不出现传统文化解读帮助入口。

#### P-12 我的手串

- 页面目标：让多手串用户先选择手串，再查看对应证书。
- 适用角色：生产环境为已授权且拥有绑定手串的用户；原型使用三条演示数据。
- 页面入口：P-04 `证书数量`。
- 业务规则：导航标题下直接开始全宽列表，不展示眉题、大标题、数量说明或引导文案。
- 字段与内容：缩略图、名称、证书编号、规格、绑定状态和右侧箭头。
- 操作与结果：整行进入所选 P-07；返回保留列表位置。
- 异常与空状态：无绑定手串时展示空态和识别说明；失败提供重试。
- 页面验收：原型恰好三条且编号唯一；触控高度不小于 76px；双设备无横向溢出。

#### P-13 手串档案

- 页面目标：展示首页藏品中的手串档案和电子证书。
- 页面入口：首页“我的藏品”手串卡片。
- 字段与内容：手串图、名称、档案编号、材质、规格、状态和 PDF。
- 操作与结果：下载成功提示开始下载；失败保留页面并提示重试。
- 页面验收：档案数据与首页卡片一致，文件名与藏品一致。

#### P-14 认种档案

- 页面目标：展示认种树档案、生长状态和证书。
- 页面入口：首页树藏品或 `认种沉香树` 服务。
- 字段与内容：树名称、认种编号、基地、树龄、日期、生长时间线和证书。
- 操作与结果：下载证书；`前往认种` 只提示商城即将上线。
- 页面验收：时间线顺序正确，手串证书与认种证书不可互换。

#### P-15 源头产业园

- 页面目标：用图集和视频介绍产区、育苗、加工与品控。
- 页面入口：首页产业园 Banner。
- 业务规则：原型使用四张图和模拟播放；生产使用已审核素材和真实媒体。
- 字段与内容：万亩基地、标准化育苗、无尘加工、全链路品控、页码和视频时长。
- 操作与结果：横向浏览；播放/暂停视频。
- 异常与空状态：单图或视频失败不阻塞其他图文。
- 页面验收：图集 4 项、页码同步、视频状态可切换。

#### P-16 沉香小知识文章

- 页面目标：以接近微信公众号的长文结构展示两篇知识内容。
- 页面入口：首页两条知识行。
- 业务规则：两篇文章独立；保养文章不替代 P-08 七则手册。
- 字段与内容：头图、标题、`广垦沉香 / 日期 / 原创`、导语、分节正文和结尾说明。
- 操作与结果：只支持滚动阅读和返回。
- 页面边界：无点赞、评论、分享、收藏、输入、发布或底部互动栏。
- 页面验收：标题与头图正确；正文无交互控件；双设备长文滚动无溢出。

#### P-17 商城占位

- 页面目标：明确商城尚未开放，避免空白或伪交易流程。
- 页面入口：根商城 Tab、甄选四项、查看更多和认种行动。
- 业务规则：只显示 `即将上线，敬请期待` 轻提示。
- 页面验收：不存在商品、购物车、订单、支付、地址或虚假购买状态。

### 8. 页面评审说明

以下 `DOC-P-xx` 用于 React 原型外“页面 PRD”全高抽屉，统一按消保委页面级 PRD 的目标、入口、规则、字段、操作结果、状态、异常和验收口径展示。抽屉只展示当前页面，不提供“当前页面 / 完整 PRD”切换。

#### DOC-P-01 首页

- **页面目的**：集中承载品牌、AI 主入口、甄选预告、藏品、服务、产业园和知识内容。
- **入口与出口**：芯片识别或根 Tab 进入；可进入 P-02/P-03/P-07 至 P-10/P-13 至 P-17。
- **内容与交互**：按确认顺序纵向滚动；当前证书直达；商城入口只提示即将上线。
- **状态与权限**：公开内容匿名可见；问帖需要授权；单模块失败不阻塞整页。
- **数据与接口**：当前手串、藏品摘要、内容摘要和产业园素材。
- **验收条件**：AC-ID、AC-HOME、AC-COLLECTION、AC-CONTENT 和 AC-COMPAT。

#### DOC-P-02 登录确认层

- **页面目的**：解释微信授权用途并恢复来源意图。
- **入口与出口**：问帖、记录或主动授权进入；成功后执行 pending_intent 或就地更新。
- **内容与交互**：手机内 Bottom Sheet，包含说明、确认和取消。
- **状态与权限**：未授权、授权中、已授权、失败；全局共享业务会话。
- **数据与接口**：pending_intent、微信 code 和业务 session。
- **验收条件**：AC-AUTH。

#### DOC-P-03 AI问事

- **页面目的**：以新对话空态和连续文字消息完成主动追问与结构化参考。
- **入口与出口**：首页、根问帖 Tab或历史详情进入；每次均为新会话。
- **内容与交互**：Logo、快捷主题、文字输入、发送、思考、判断/提醒/建议和免责声明。
- **状态与权限**：必须授权；覆盖空态、输入、生成、失败、安全转向和重置。
- **数据与接口**：Conversation、Message、AISafetyDecision；无图片与音频数据。
- **验收条件**：AC-AI、AC-SAFE、AC-AUTH 和 AC-COMPAT。

#### DOC-P-04 我的

- **页面目的**：用全宽账号 Banner集中授权状态、问帖记录、证书数量和芯片说明。
- **入口与出口**：根 Tab `我的`；可进入 P-02、P-05、P-11 和 P-12。
- **内容与交互**：账号横排；数量在上、标题在下；Banner 下只保留芯片说明。
- **状态与权限**：记录需要授权；生产手串列表需要授权；原型展示模拟数量。
- **数据与接口**：Session、记录计数和绑定手串计数。
- **验收条件**：AC-PROFILE、AC-AUTH、AC-HISTORY 和 AC-CERT。

#### DOC-P-05 问帖记录

- **页面目的**：按最近更新时间查看历史问帖。
- **入口与出口**：P-04 进入；记录进入 P-06，空态进入新 P-03。
- **内容与交互**：标题、相对时间、摘要和生产 cursor 分页。
- **状态与权限**：必须授权；覆盖加载、空态、分页和失败重试。
- **数据与接口**：Conversation 摘要列表。
- **验收条件**：AC-HISTORY 和 AC-AUTH。

#### DOC-P-06 问帖详情

- **页面目的**：只读查看历史上下文并开启新问帖。
- **入口与出口**：P-05 进入；`开启新问帖` 创建新会话进入 P-03。
- **内容与交互**：历史消息只读，不展示输入框，不续接旧会话。
- **状态与权限**：仅记录所有者可见；不存在与越权统一处理。
- **数据与接口**：Conversation 和 Message 只读详情。
- **验收条件**：AC-HISTORY 和 AC-AI。

#### DOC-P-07 证书与溯源

- **页面目的**：展示当前或所选手串的数字身份和时间线。
- **入口与出口**：首页直达当前手串；P-12 选择后进入；返回原来源。
- **内容与交互**：名称、编号、产区、香韵、核验状态和时间线来自同一记录。
- **状态与权限**：有效扫描会话匿名可见；账号绑定列表需生产鉴权。
- **数据与接口**：Bracelet、Certificate 和 TimelineEvent。
- **验收条件**：AC-ID、AC-CERT 和 AC-CONTENT。

#### DOC-P-08 佩戴养护

- **页面目的**：展示七则完整日常养护指南。
- **入口与出口**：首页服务进入并返回。
- **内容与交互**：编辑式题图、七则正文和养护提醒，只读展示。
- **状态与权限**：匿名可见；支持版本缓存与重试。
- **数据与接口**：Content `care`。
- **验收条件**：AC-CONTENT 和 AC-COMPAT。

#### DOC-P-09 沉香知识

- **页面目的**：展示三项基础知识和唯一农垦故事入口。
- **入口与出口**：首页服务或查看更多进入；可进入 P-10。
- **内容与交互**：形成、闻香、产区差异和农垦故事。
- **状态与权限**：匿名可见；支持版本缓存与重试。
- **数据与接口**：Content `knowledge`。
- **验收条件**：AC-CONTENT。

#### DOC-P-10 农垦故事

- **页面目的**：介绍产区环境、农垦积淀和从林场到手串。
- **入口与出口**：从 P-09 唯一入口进入并返回。
- **内容与交互**：三段品牌故事，只读展示。
- **状态与权限**：匿名可见；不混入本串证书时间线。
- **数据与接口**：Content `farm-story`。
- **验收条件**：AC-CONTENT。

#### DOC-P-11 芯片识别说明

- **页面目的**：解释 NFC 识别步骤与失败重试。
- **入口与出口**：从 P-04 或 P-00 进入并返回。
- **内容与交互**：三步说明及“芯片不会直接开始问帖”。
- **状态与权限**：匿名可见；不支持 NFC 时提示替代路径。
- **数据与接口**：Content `chip-help`。
- **验收条件**：AC-ID 和 AC-ERROR。

#### DOC-P-12 我的手串

- **页面目的**：让账号先选择已绑定手串，再查看对应证书。
- **入口与出口**：从 P-04 证书数量进入；选择后进入 P-07，返回保留列表位置。
- **内容与交互**：导航标题下直接展示全宽手串列表，无额外标题或说明。
- **状态与权限**：生产环境必须授权；覆盖加载、空态、失败和分页。
- **数据与接口**：UserBraceletBinding、Bracelet、Certificate 摘要；`GET /v1/users/me/bracelets`。
- **验收条件**：AC-CERT 和 AC-COMPAT。

#### DOC-P-13 手串档案

- **页面目的**：展示首页藏品中的手串信息和电子证书。
- **入口与出口**：首页藏品进入并返回。
- **内容与交互**：手串图片、编号、材质、规格、状态、查看证书和下载。
- **状态与权限**：演示环境匿名可见；下载失败保留页面并允许重试。
- **数据与接口**：Bracelet、Certificate 和 PDF 文件服务。
- **验收条件**：AC-COLLECTION 和 AC-ERROR。

#### DOC-P-14 认种档案

- **页面目的**：展示认种树信息、生长时间线和认种证书。
- **入口与出口**：首页藏品或认种服务进入；认种行动只提示商城未开放。
- **内容与交互**：树档案、基地、树龄、日期、时间线、证书下载。
- **状态与权限**：演示环境匿名可见；正式环境按认种关系鉴权。
- **数据与接口**：AdoptionRecord、GrowthEvent 和 PDF 文件服务。
- **验收条件**：AC-COLLECTION 和 AC-ERROR。

#### DOC-P-15 源头产业园

- **页面目的**：通过四图图集和视频展示种植、育苗、加工与品控。
- **入口与出口**：首页产业园 Banner 进入并返回。
- **内容与交互**：横向图集、同步页码、播放与暂停。
- **状态与权限**：匿名可见；单个媒体失败不阻塞其他内容。
- **数据与接口**：Content `industrial-park` 和媒体服务。
- **验收条件**：AC-PARK、AC-CONTENT 和 AC-ERROR。

#### DOC-P-16 沉香小知识文章

- **页面目的**：以公众号式长文结构展示两篇沉香知识文章。
- **入口与出口**：首页两条知识行进入并返回。
- **内容与交互**：头图、标题、作者时间、原创标识、导语、分节正文和结尾说明，只读展示。
- **状态与权限**：匿名可见；无互动、输入或发布权限。
- **数据与接口**：Content `knowledge-authenticity` 与 `knowledge-care`。
- **验收条件**：AC-CONTENT 和 AC-COMPAT。

## User Stories

### 1. P0 用户故事

- `US-001` 作为手串用户，我通过芯片入口只看到凭证对应的真实手串身份。
- `US-002` 作为首页用户，我可以直接查看当前手串证书。
- `US-003` 作为多手串用户，我先选择手串，再查看所选证书，避免错配。
- `US-004` 作为未授权用户，我理解授权用途，授权后能继续刚才的操作。
- `US-005` 作为问帖用户，我每次进入都从新对话开始并可自由输入。
- `US-006` 作为用户，我先收到必要追问，再获得判断、提醒和建议。
- `US-007` 作为隐私敏感用户，我不会被要求上传图片、文件、语音、生辰或精确位置。
- `US-008` 作为回访用户，我可以只读查看历史，并从详情开启新对话。
- `US-009` 作为高风险问题用户，我会收到安全转向和现实求助建议。
- `US-010` 作为内容用户，我可以匿名阅读证书、养护、知识、产业园和文章。

### 2. P1 用户故事

- `US-011` 我可以在手串与认种树之间切换、查看档案并下载对应证书。
- `US-012` 我可以通过产业园图集和视频了解种植、育苗、加工和品控。
- `US-013` 我可以像阅读公众号一样浏览知识文章，不被互动控件打断。
- `US-014` 弱网或失败时，我不会丢失已成功内容或刚输入的文字。
- `US-015` 商城未开放时，我能得到明确提示而不会进入伪交易流程。

## Implementation Decisions

### 1. 系统边界

- C1 微信小程序客户端：页面、路由、微信登录、NFC 参数、文字输入、状态和本地安全缓存。
- C2 业务 API：鉴权、手串解析、绑定、证书、内容、会话、记录和埋点。
- C3 AI Orchestrator：输入安全、追问判定、模型调用、结构校验、输出安全和模型日志。
- C4 内容服务：养护、知识、农垦故事、产业园和文章版本管理。
- C5 文件与媒体：电子证书 PDF、图片和视频；原型静态文件不作为生产数据源。
- C6 外部依赖：微信登录、芯片/NFC、模型供应商和内容审核。

### 2. 功能需求

#### 身份识别

- `FR-ID-001 P0` 客户端必须读取 scan_token 并调用解析接口。
- `FR-ID-002 P0` 服务端必须校验 token、手串、证书和签发主体状态。
- `FR-ID-003 P0` 无效或缺失 token 不得返回演示手串。
- `FR-ID-004 P0` 核验标签必须由服务端按核验强度返回。
- `FR-ID-005 P0` 有效扫描会话查看当前手串证书不得要求业务登录。

#### 登录与权限

- `FR-AUTH-001 P0` 基础登录使用 wx.login code 换取服务端 session。
- `FR-AUTH-002 P0` 基础使用不得强制获取头像、昵称或手机号。
- `FR-AUTH-003 P0` 问帖、问帖记录和账号绑定手串列表要求有效 session。
- `FR-AUTH-004 P0` 登录成功恢复 pending_intent；主动授权只就地更新状态。
- `FR-AUTH-005 P0` 首页、问帖和我的共享同一授权状态。

#### 首页与商城

- `FR-HOME-001 P0` 首页模块顺序必须与 P-01 一致。
- `FR-HOME-002 P0` 四项服务整格可点击并进入真实目的地。
- `FR-HOME-003 P0` 商城相关入口只能显示统一即将上线提示。
- `FR-HOME-004 P1` 单个模块失败不得阻断首页其他模块。

#### AI 会话

- `FR-AI-001 P0` 每次正常进入 P-03 创建新 conversation，不恢复旧会话。
- `FR-AI-002 P0` Agent 按信息充分度返回 CLARIFY、ANSWER 或 SAFE_REDIRECT。
- `FR-AI-003 P0` ANSWER 包含非空 judgment、reminder 和 suggestion。
- `FR-AI-004 P0` Agent 不得使用手串身份信息作命运推断。
- `FR-AI-005 P0` 页面只允许文字输入与发送，不接受图片、文件或音频。
- `FR-AI-006 P0` 请求中禁止重复提交，失败保留消息与草稿。
- `FR-AI-007 P0` Agent 输出保存模型版本、安全判定和免责声明版本。

#### 历史与证书

- `FR-HISTORY-001 P0` 历史列表按 updated_at DESC 分页。
- `FR-HISTORY-002 P0` 详情按 sequence_no 只读展示消息。
- `FR-HISTORY-003 P0` 详情后续行动创建新 conversation，不复用旧 conversation_id。
- `FR-HISTORY-004 P0` 记录读取校验用户所有权。
- `FR-CERT-001 P0` 账号可绑定多串手串，每串最多关联一个当前有效证书。
- `FR-CERT-002 P0` 列表返回唯一 bracelet_id、certificate_id、名称、编号、规格、图片和状态。
- `FR-CERT-003 P0` 详情按 selected_bracelet_id 读取，禁止复用默认首条数据。
- `FR-CERT-004 P0` 首页 current_bracelet_id 与 Profile selected_bracelet_id 是两个明确上下文。
- `FR-CERT-005 P0` 证书字段缺失不得由客户端或 AI 补写。

#### 内容、藏品与媒体

- `FR-CONTENT-001 P1` 内容必须带 content_version 与 updated_at。
- `FR-CONTENT-002 P1` 内容失败优先显示成功缓存，无缓存时重试。
- `FR-CONTENT-003 P0` 养护手册与保养文章使用不同 content_key 和模板。
- `FR-CONTENT-004 P0` 展示型文章不得渲染互动、输入或发布控件。
- `FR-COLLECTION-001 P1` 手串与认种树档案绑定各自数据和证书文件。
- `FR-COLLECTION-002 P1` 下载失败可重试且不能误报成功。
- `FR-PARK-001 P1` 产业园图集与视频使用已审核素材和内容版本。

### 3. 状态模型

- 会话状态：ANONYMOUS、LOGIN_REQUIRED、AUTHENTICATING、AUTHENTICATED、AUTH_ERROR、SESSION_EXPIRED。
- AI 状态：NEW、SUBMITTING、CLARIFYING、GENERATING、READY、SAFE_REDIRECT、FAILED_RETRYABLE。
- 内容状态：LOADING、READY、STALE_CACHE、ERROR_EMPTY、MEDIA_UNAVAILABLE。
- 任意受保护接口均由服务端再次鉴权，客户端状态不构成权限证明。

### 4. 数据要求

- User：id、wechat_openid_hash、status、privacy_consent_version、created_at、updated_at。
- Bracelet：id、serial_no、display_name、spec、production_year、origin、scent_profile、status、hero_image_url。
- UserBraceletBinding：user_id、bracelet_id、binding_status、bound_at、source。
- Certificate：id、certificate_no、bracelet_id、issuer_name、issued_at、verification_status、pdf_url。
- TimelineEvent：id、bracelet_id、event_type、title、occurred_at、location、description。
- AdoptionRecord：id、tree_no、base_name、tree_age、adopted_at、growth_status、certificate_url。
- Conversation：id、user_id、title、topic、status、created_at、updated_at。
- Message：id、conversation_id、sequence_no、role、content、agent_action、model_version、safety_category、created_at。
- Content：content_key、title、hero_image_url、sections、content_version、updated_at、publish_status。
- Article：article_id、title、author、published_at、original_flag、hero_image_url、lead、sections、closing_note。
- 原始 openid、scan_token 和服务端密钥不得写入普通日志。

### 5. API 合同

#### 通用约定

- 基础路径 `/v1`；UTF-8；时间使用 ISO-8601 UTC 并按 Asia/Shanghai 展示。
- 受保护接口使用 Bearer session_token；写接口支持 Idempotency-Key。
- 成功返回 data 与 request_id；失败返回 code、message、retryable、request_id 和 details。

#### `POST /v1/auth/wechat-session`

- 请求：code、privacy_consent_version。
- 返回：user_id、session_token、expires_at、is_new_user。

#### `GET /v1/bracelets/resolve?scan_token={token}`

- 权限：匿名。
- 返回：bracelet、certificate_summary、verification_label、verification_level 和 scan_session。

#### `GET /v1/users/me/bracelets`

- 权限：已登录。
- 返回：items、next_cursor、has_more；items 含 bracelet_id、certificate_id、display_name、serial_no、spec、thumbnail_url 和 binding_status。

#### `GET /v1/bracelets/{bracelet_id}`

- 权限：有效扫描会话或当前用户已绑定。
- 返回：Bracelet 公开字段和当前证书摘要。

#### `GET /v1/certificates/{certificate_id}`

- 权限：同对应手串读取权限。
- 返回：Certificate 与按 occurred_at ASC 的 TimelineEvent。

#### `GET /v1/content/{content_key}`

- content_key：care、knowledge、farm-story、chip-help、industrial-park、knowledge-authenticity、knowledge-care。
- 权限：匿名。
- 返回：title、hero_image_url、content_version、updated_at 和 sections。

#### `POST /v1/conversations`

- 权限：已登录。
- 请求：entry_source 为 HOME、TAB、HISTORY_EMPTY 或 HISTORY_DETAIL。
- 返回：新的 Conversation；同一 Idempotency-Key 不重复创建。

#### `POST /v1/conversations/{conversation_id}/messages`

- 权限：已登录且为所有者。
- 请求：content 为 1–500 字符，client_message_id 为 UUID。
- 顺序：所有权校验、幂等检查、保存用户消息、输入审核、Agent、输出审核、保存 Agent 消息。

#### `GET /v1/conversations?cursor={cursor}&limit=20`

- 权限：已登录。
- 返回：当前用户会话摘要、next_cursor 和 has_more，按 updated_at DESC。

#### `GET /v1/conversations/{conversation_id}`

- 权限：已登录且为所有者。
- 返回：Conversation 与按 sequence_no ASC 的 messages。
- 不存在与非所有者统一返回 CONVERSATION_NOT_FOUND。

### 6. Agent 与非功能规格

- 输入允许：系统规则、当前新会话消息、当前时间、语言和安全策略版本。
- 输入禁止：手串编号、材质、产区、年份、香韵、证书、openid、手机号、精确地址、生辰、面相和掌纹。
- ANSWER 必须包含 judgment、reminder、suggestion；SAFE_REDIRECT 必须包含现实帮助方向。
- 对确定性预测、改运、医疗、法律、金融、自伤、暴力、隐私和提示词注入按安全策略转向或阻断。
- 首屏可交互 P75 不高于 2.5 秒；身份解析 P95 不高于 1.2 秒；AI 首个可见反馈不高于 1 秒。
- iPhone 与 Pixel 不得横向溢出；触控区域不小于 44×44 CSS px；固定导航和输入区不得遮挡内容。
- 日志不得记录原始 openid、scan_token、会话全文、模型密钥或供应商错误栈。

## Testing Decisions

### 1. 验收用例

#### 身份、首页与商城

- `AC-ID-001` 有效 SECURE_CHIP token 解析后展示对应 serial_no 与服务端核验标签。
- `AC-ID-002` 无效 token 不展示演示手串，并提供重试和识别说明。
- `AC-ID-003` 撤销证书不得继续显示已核验。
- `AC-HOME-001` 首页八个模块按确认顺序展示且可滚动。
- `AC-HOME-002` 四项服务分别进入当前证书、养护、知识和认种档案。
- `AC-HOME-003` 商城相关入口只显示即将上线，不发生页面跳转。

#### 授权与我的

- `AC-AUTH-001` 匿名用户开始问帖时打开手机内授权层。
- `AC-AUTH-002` 授权成功自动恢复来源意图，取消不创建会话。
- `AC-AUTH-003` Profile 主动授权只就地切换登录状态。
- `AC-PROFILE-001` Banner 通铺状态栏，账号同一横排，数据入口位于下半部。
- `AC-PROFILE-002` Profile 下方只存在芯片说明，不存在重复入口或传统文化帮助。
- `AC-PROFILE-003` 双设备完整页面位于固定根 Tab 之上。

#### AI 对话

- `AC-AI-001` 每次进入 P-03 都显示全新空态和三个快捷主题。
- `AC-AI-002` 快捷主题或文字发送后切换为连续消息流。
- `AC-AI-003` Agent 先追问，再输出判断、提醒和建议。
- `AC-AI-004` 空内容和思考中重复点击不得创建消息。
- `AC-AI-005` 离开并重新进入后不恢复上一段会话。
- `AC-AI-006` 页面不存在图片、文件、附件、相机、麦克风、语音或录音入口。
- `AC-AI-007` 免责声明持续可见，最后消息不被输入区或键盘遮挡。
- `AC-SAFE-001` 确定性预测、改运和专业高风险问题安全转向。

#### 历史、证书与内容

- `AC-HISTORY-001` 未授权进入记录前必须授权。
- `AC-HISTORY-002` 列表点击进入只读详情，不直接进入新对话。
- `AC-HISTORY-003` 详情无输入框，开启新问帖进入全新空态。
- `AC-CERT-001` Profile 的 3 串入口先进入 P-12。
- `AC-CERT-002` P-12 直接展示三条全宽记录，无眉题、大标题或说明。
- `AC-CERT-003` 第二条进入后显示 CX-2024-116 和对应详情。
- `AC-CERT-004` 首页证书仍直达 CX-2018-072 当前详情。
- `AC-COLLECTION-001` 藏品可在手串和认种树之间切换，分页同步。
- `AC-COLLECTION-002` 两份电子证书与档案对象对应，失败可重试。
- `AC-CONTENT-001` 养护页完整展示七则和提醒。
- `AC-CONTENT-002` 沉香知识展示三个主题和唯一农垦故事入口。
- `AC-CONTENT-003` 两篇文章标题、头图和正文对应，正文无交互控件。
- `AC-PARK-001` 产业园图集 4 项，页码同步，视频状态可切换。
- `AC-ERROR-001` 单个内容或媒体失败不清空其他成功内容。
- `AC-COMPAT-001` 双设备无横向溢出、固定导航遮挡或安全区碰撞。

### 2. 自动化与完成标准

- 单元测试覆盖状态、证书映射、内容 key、AI 输出结构和安全策略。
- 合同测试覆盖认证、解析、绑定列表、证书、内容和会话 API。
- 端到端测试覆盖首页入口、多手串、新问帖重置、历史只读、藏品下载和产业园。
- 双设备测试使用 iPhone 393×852 与 Pixel 427×952。
- P0 验收全部通过，PRD、原型、页面文案、接口和测试不存在冲突。
- 微信类目、隐私、AI 安全、证书签发主体和品牌内容完成责任人确认后才可发布。

## Scope Priority

### 1. Must / P0

- 当前手串识别、证书与溯源。
- 微信授权、意图恢复和全局授权状态。
- 新问帖、纯文字对话、主动追问、结构化回答和安全转向。
- 问帖记录列表与只读详情。
- 多手串列表与证书一一对应。
- 首页完整模块、四项服务和商城占位边界。
- 免责声明、隐私、权限和关键错误状态。

### 2. Should / P1

- 七则养护、沉香知识、农垦故事和两篇公众号式文章。
- 藏品轮播、手串/认种档案、电子证书下载。
- 产业园四图图集和视频。
- 内容版本、成功缓存、埋点和双设备可用性。

### 3. Could / P2

- 正式商城、认种购买、更多文章、藏品搜索排序和证书分享。
- 内容、证书、绑定、产业园素材和 AI 质量运营后台。

### 4. 依赖、风险与里程碑

- 芯片与证书：品牌方/供应链；未确认则阻塞身份核验上线。
- 微信登录与类目：小程序负责人；未确认则阻塞授权和问帖。
- AI 模型与安全：AI/安全负责人；未通过评测则阻塞 AI 上线。
- 品牌内容与产业园：内容负责人；未审核则只能使用占位内容。
- 隐私与免责声明：法务；未确认则阻塞授权和会话留存。
- 多证书错配风险：生产必须使用 bracelet_id/certificate_id，不以数组位置作主键。
- 原型误当生产风险：评审逐项标记模拟授权、模拟视频、静态 PDF 和本地 AI 回复。
- M1 需求冻结；M2 数据接口冻结；M3 原生小程序联调；M4 安全内容验收；M5 灰度发布。

## Out of Scope

- 不建设真实商城、商品、购物车、订单、支付、地址、售后或认种购买。
- 不支持图片识别、拍照鉴香、文件上传、语音输入、录音、视频通话或数字人对话。
- 不提供确定性预测、改运、法事、医疗、法律、金融或心理诊断。
- 不支持证书解绑、转赠、交易、公开分享、搜索、筛选或详情内证书切换。
- 不建设点赞、评论、收藏、转发、文章发布或社区能力。
- 不建设完整运营后台；相关能力另立后台 PRD。
- React 原型不包含真实数据库、真实微信授权、真实 NFC、远程 AI 或生产监控。

## Further Notes

### 1. Definition of Ready

- 页面范围、入口、角色、规则、字段、操作、异常和验收已明确。
- 当前手串与账号绑定手串的数据上下文已区分。
- 微信授权、证书读取、内容读取和 AI 会话权限已确认。
- 外部依赖有明确负责人和联调输入。

### 2. 发布阻塞项

- 微信小程序类目、服务名称和 AI 传统文化解读文案预审。
- 芯片安全等级、证书签发主体、撤销机制和客服查询能力确认。
- 隐私政策、会话保留期、账号删除和未成年人边界确认。
- AI 输入/输出安全、敏感场景、安全转向和人工升级方案验收。
- 养护内容中的术语、安全性和品牌责任确认。

### 3. 当前原型与生产差异

- 原型授权只在本地切换，不读取真实微信数据。
- 原型固定展示 1 条问帖、3 串证书、两件藏品和本地模拟 AI 回复。
- 原型 PDF、产业园视频进度、文章和时间线均为演示资产。
- 原型商城只显示轻提示，未建设交易能力。
- 生产版本必须按本文 API、权限、数据和错误状态实现，不得复制静态数组。

### 4. 修订记录与相关文档

- 2.0，2026-08-24：建立数字身份、AI Agent、接口、安全和验收真源。
- 3.0，2026-08-31：按消保委页面级 PRD 重构；纳入滚动首页、四 Tab、藏品与认种、产业园、多手串、Profile Banner、纯文字新问帖、七则养护和公众号式文章；删除语音、旧对话续接、传统文化帮助和单证书直跳等过期结论。
- 客户补充首页：`docs/superpowers/specs/2026-08-28-customer-feedback-home-expansion-design.md`。
- 问帖：`docs/superpowers/specs/2026-08-31-chatgpt-style-question-flow-design.md`。
- Profile：`docs/superpowers/specs/2026-08-31-profile-simple-list-design.md`。
- 多手串：`docs/superpowers/specs/2026-08-31-multi-certificate-list-design.md`。
- 养护：`docs/superpowers/specs/2026-08-31-care-editorial-manual-design.md`。
- 知识文章：`docs/superpowers/specs/2026-08-31-knowledge-wechat-article-design.md`。
- 参考写法：`/Users/xinwei/weiran-env/knowledge-hub/10-项目/杭小消/docs/codex/prd/2026-08-27-hangxiaoxiao-admin-page-prd.md`。
