# Mobile Prototype Agent Guide

## Page PRD fidelity note (2026-08-31)

The browser-level page PRD must faithfully match the supplied 杭小消/消保委 review component, not the older top toolbar or Markdown-excerpt drawer. Its entry is the fixed 44×92 vertical `页面 / PRD` tab at the middle-right edge. Opening it shows a blocking page mask and a 720px full-height right drawer with a page-specific title, scrollable field-level body, and fixed footer; it closes from the header, footer, mask, or Esc and then returns focus to the entry. Each mapped page needs its own `页面名 · 页面 PRD` title, status/key/route/update metadata, page overview, page-level business rules, field table, action/result table, product boundary, state/navigation flow, notifications/logging, exceptions/empty states, acceptance criteria, and technical assessment items. Do not restore the top-right horizontal `产品文档` button, the `当前页面 / 完整 PRD` switch, or the former Markdown excerpt sections unless a later explicit decision overrides this note.

## Knowledge article fidelity note (2026-08-31)

The two homepage rows under `沉香小知识` open separate display-only article details styled after a WeChat Official Account reading page: wide hero image, article title, `广垦沉香 / 日期 / 原创` metadata, lead, section headings, body paragraphs, and a quiet closing note. Do not add likes, comments, share, favorite, input, or bottom action controls unless a later explicit decision overrides this note. The homepage service shortcut `佩戴养护` remains a separate direct route to the seven-part care manual.

## Profile simple-list fidelity note (2026-08-31)

The latest selected `我的` visual source is `reference/profile-simple-list-selected.png`, which supersedes the 2026-08-24 profile-card direction. The later 2026-08-31 refinement requires the WeChat authorization area to be a distinct product-photo Banner using the text-free `public/assets/profile/profile-account-banner.png`, with real HTML account copy and authorization controls layered over it; it must read as the page focus, extend behind the status/battery area, and bleed to both screen edges. Avatar, account copy, and `微信授权登录` stay on one upper horizontal row with the login/status control right-aligned; clicking login must first open the phone-scoped WeChat authorization sheet. The Banner lower area owns two clickable data entries for `问帖记录` and `证书数量`; each shows quantity above title with a 7px gap, so do not repeat `问帖记录` or `本串证书` in the lower list. `证书数量` displays `3 串` in the prototype and uses the list-first path `我的 → 我的手串（3 条）→ 证书与溯源详情`; the homepage certificate service remains a direct shortcut to the current bracelet. The lower 18px-inset list contains only the single-line `芯片识别说明` entry; do not restore `关于传统文化解读` on Profile. Do not restore the `个人香事档案` title, `记录与管理` or `使用帮助` headings, the deep-green split action block, separate help cards, metrics, or extra profile features. Keep existing authorization and retained route behavior, use 44/48px mobile touch targets, and fit the complete page above the fixed tabs on iPhone and Pixel.

## Care editorial fidelity note (2026-08-31)

The selected care-page source of truth is `reference/care-editorial-manual-selected.png`, with `public/assets/home-reference/care-editorial-hero-v3.png` as the text-free hero composition. Preserve the compact ivory editorial poster density, lower-left bracelet, upper-right bamboo, copper serif numbering, and restrained reminder bar when changing the care page.

## AI question-page fidelity note (2026-08-31)

The selected new-conversation source is `reference/chatgpt-question-new-logo-selected.png`; the selected active-conversation source is `reference/chatgpt-question-conversation-selected.png`. Every normal entry starts fresh. Use the official `public/assets/home-reference/guangken-chenxiang-logo.png` only in the empty state, then switch to a clean ChatGPT-style message stream. The composer is text-and-send only: never add upload, attachment, camera, microphone, voice, waveform, or audio controls unless a later explicit decision overrides this note. Keep the composer/input visibly inset from both phone edges (currently 14px each side); do not let it touch or clip against the device frame.

## Prototype Instructions

In ChatGPT Work Mode, run `sites-preview start "$PWD"`, open `http://terminal.local:4173/` in the cloud browser, and verify the rendered app and its primary interactions. Keep that preview open and tell the user to inspect it in the cloud browser; do not present the local URL as a user-facing chat link. In Codex Desktop, run the local server yourself, open the preview in the in-app browser, and provide the clickable local URL. Do not deploy to Sites unless the user explicitly asks to share, publish, or deploy. Do not give the user server-start instructions when you can run it.

Before planning or implementing any mobile-app change, read this `AGENTS.md` in full. It is the source of truth for the template's runtime and component guidance.

Before making substantial visual changes, use the Product Design plugin's `get-context` skill when the visual source is unclear or no longer matches the current goal. When the user gives durable prototype-specific design feedback, preferences, or decisions, record them in `AGENTS.md`.

When implementing from a selected generated mock, treat that image as the source of truth for layout, component anatomy, density, spacing, color, typography, visible content, and hierarchy.

## Editing Boundary

- Build app-specific UI in `src/Prototype.tsx` and `src/prototype.css`.
- Treat `src/App.tsx`, `src/main.tsx`, `src/styles.css`, `src/mobile/`, `public/assets/iphone/`, `public/assets/android/`, `public/assets/status/`, `vite.config.ts`, `worker/index.js`, and `scripts/prepare-sites-build.mjs` as protected runtime files. Do not edit, replace, remove, or recreate them unless the user explicitly asks to change the mobile runtime itself. For an explicit runtime change, update the affected lock hashes only after verifying the new runtime behavior.
- Run `npm run check:runtime` before preview or handoff. If it fails, restore the protected runtime instead of weakening or bypassing the check.
- `npm run build` preserves the mobile runtime and prepares the static Cloudflare Worker output required by Sites. Before a Sites handoff, confirm `dist/client/index.html`, `dist/server/index.js`, `dist/.openai/hosting.json`, and source `.openai/hosting.json` exist, then run `npm run test:sites`. Do not replace this project with a Vinext starter.

## Runtime Contract

- Preserve the mobile device runtime unless the user's task explicitly asks otherwise. Do not replace it with a standalone page. Visual fidelity applies to app-owned content inside the device screen, not to template-owned device chrome.
- Keep `App` composed around `PhoneFrame` -> `KeyboardProvider`, with `StatusBar`, app content, `HomeIndicator`, and `KeyboardDock` mounted inside the phone frame. `StatusBar` and the iOS home indicator are overlaid device chrome. When the Android keyboard is closed, the app viewport reserves the protected navigation-bar region instead of painting behind it. When the Android keyboard is open, preserve the current full-screen keyboard layout: its asset includes the IME navigation strip and the separate black navigation bar is hidden. iOS screens continue to paint behind the home-indicator area and own their safe-area content padding.
- Preserve the `iPhone` / `Pixel 10` device picker and both calibrated device presets. The Pixel screen is `427 x 952`; its `32 x 32` camera circle and `public/assets/android/navigation-bar.svg` bottom navigation bar are protected device chrome, not app content.
- Preserve the device picker's intentionally lightweight Codex styling in the top-right corner: its trigger wrapper is borderless and transparent, its trigger sizes to content, and its right-aligned menu uses the compact 3px inset plus the specified hairline and elevation shadow layers. Keep the prototype root and default app screen white.
- Preserve `StatusBar` as live device chrome, including its platform-specific typography, source status-icon assets, and spacing. Pixel 10 uses Roboto, Android indicators, and 32px top, left, and right padding. iPhone uses its iOS indicators, system typography, and calibrated spacing. Do not hardcode screenshot times like `9:41` into the status bar, replace its real-time clock, or move status bar content into app markup unless the user explicitly asks for a fixed/mock device time.
- `PhoneFrame` owns the calibrated device frame, screen portal, device picker, camera cutout, and custom cursor. Keep device assets in `public/assets/iphone/` and `public/assets/android/`; if an asset fails to load, repair the asset path or restore the asset instead of removing the frame, keyboard, or image render.
- Use `MobileScroll` directly for simple single-screen prototypes. Use `FlowStack` for conventional multi-screen flows whose routes can own their fixed header and footer; when using it, define each route as a `FlowScreen`: `{ id, header?, headerHeight?, footer?, footerHeight?, render }`, and use `flow.push(screen)`, `flow.pop()`, and `flow.replace(screen)` from `FlowStack` render callbacks or `useFlow()` instead of introducing another router.
- Use `Carousel` for a carousel, horizontal rail, swipeable cards, image or media strip, horizontally scrollable cards, chip rail, or other horizontal collection.
- For a layered app shell—such as a persistent composer, independently presented sheet, pushed/peek sidebar, or app-wide transition—compose directly in `Prototype.tsx` rather than forcing it through `FlowStack`. Keep app-owned fixed chrome as sibling layers outside `MobileScroll`.
- When using `FlowScreen`, put route-owned fixed headers or footers in `FlowScreen.header` or `FlowScreen.footer`. Set `headerHeight` to the visible app-toolbar height; `FlowStack` adds the device's top safe-area/status-bar inset automatically. Do not include `StatusBar` or its height in the header. Set `footerHeight` to the full app-footer height. `FlowScreen.footer` is an overlay, not reserved layout space; screens using it must add their own bottom content padding such as `padding-bottom: calc(var(--flow-footer-height) + var(--mobile-safe-area-height) + 24px)` so final content can scroll above the footer while still painting behind it.
- Render only scrollable content inside `MobileScroll`; it is for content that should move with scroll and rubber-band overscroll. Keep app-owned headers, nav bars, tabs, composers, and overlays outside it. This keeps scroll physics, safe areas, keyboard insets, scrollbars, and drag click suppression active without letting content paint under fixed chrome.
- Buttons, links, cards, and images inside `MobileScroll` should still allow drag scrolling when the pointer moves beyond tap slop. Use `data-scroll-drag="ignore"` only for rare controls that must own the drag gesture themselves.
- Do not add `var(--keyboard-height)` to ordinary screen/content padding inside `MobileScroll`; the scroll viewport already shrinks above the simulated keyboard. For custom fixed composers, search bars, or toast chrome, use `useKeyboardInsets().bottomInset`. It is relative to the app viewport: Android returns `0` while the closed-keyboard viewport already reserves navigation, then returns the keyboard height while open; iOS continues to clear the home indicator while closed and ride directly above the keyboard while open. Do not pin custom bottom chrome to `bottom: 0` or only `keyboardHeight`.
- Use `KeyboardInput`, `KeyboardTextarea`, or `MobileTextField` for every text-entry control. A raw `input` or `textarea` disconnects focus, keyboard animation, safe-area insets, and attached surfaces.
- Use `BottomSheet` for phone-scoped sheets. Its props are `open`, `onOpenChange`, `title`, optional `description`, optional `snap`, and `children`; it renders through the phone screen portal and dismisses the keyboard before opening.

## Horizontal Carousels

- Use `Carousel` for horizontally draggable cards, images, media, chips, or other horizontal collections. Do not recreate these with `overflow-x`, custom pointer handlers, or a generic div.
- `Carousel` can be nested directly inside `MobileScroll`. It owns horizontal gestures and automatically yields vertical gestures to the parent.
- Never put `data-scroll-drag="ignore"` on or around a `Carousel`; doing so prevents vertical parent scrolling when a gesture begins inside it.
- Do not add CSS scroll snapping to `Carousel`; its runtime owns momentum and release motion.
- Use `data-scroll-drag="ignore"` only when a control must prevent parent scrolling in every drag direction.

See `src/mobile/COMPONENTS.md` for the full component and gesture contract.

## Keyboard Rule

The simulated keyboard is a separate top-layer component. Before presenting anything that behaves like iOS navigation or modal UI, dismiss it first.

Call `keyboard.hide()` before:

- pushing, popping, or replacing FlowStack routes
- opening bottom sheets, action sheets, dialogs, menus, or navigation sheets
- starting transitions where the destination should not inherit text-input focus

`FlowStack` already hides the keyboard for `push`, `pop`, and `replace`. `BottomSheet` already hides it before opening. If you add new modal/sheet/navigation primitives, follow the same rule.

When a composer, search surface, or other keyboard-attached component closes, call `keyboard.hide()` in the same event before changing that component's open state. Position attached surfaces from `useKeyboardInsets()` rather than a separate timer or visibility flag so both dismiss together.

When any text-entry control loses focus, dismiss the simulated keyboard. If the control is custom or does not use the runtime's keyboard-aware fields, handle its blur event and call `keyboard.hide()` explicitly. Keep the keyboard open only when focus is moving directly to another text-entry control that should share the same keyboard session.

## Interaction Rules

- Do not trigger buttons or inputs after a pointer has become a drag. Preserve the drag suppression behavior in `MobileScroll`.
- Do not allow native browser image/file dragging inside the phone frame. Preserve the phone-level `dragstart` suppression and non-draggable image styles so scroll drags that begin on images still scroll the prototype.
- Use `KeyboardInput`, `KeyboardTextarea`, or `MobileTextField` for text entry so the simulated keyboard and safe-area insets stay connected.
- Fixed phone chrome should not animate with pushed screens. Screen content can animate; the status bar, camera cutout, and preview chrome should stay put.
- Keep the keyboard below the home indicator/safe area layer in z-index, and above ordinary app UI while visible.
- Keep the home indicator as the topmost safe-area layer in the z-index above everything else in the prototype.

## Product Direction (2026-08-20)

- Visual source of truth: `reference/b-museum-credential.png` (selected direction B).
- Use "digital identity and trusted credential" as the primary narrative, with a contemporary museum/archive tone.
- Preserve the warm Eastern material quality from direction A and expose an immediate AI question entry inspired by direction C.
- Keep the SOE-safe homepage label `AI传统文化解读`. In the requirements prototype, that entry opens a simulated conversational divination Agent: users ask freely in chat, the Agent proactively asks for missing context, then answers with a traditional-culture reading and continues the conversation. Do not turn this into a category form or a fixed `click to cast → static result` flow. The conversation may use hexagram language, but must carry a clear AI-generated entertainment disclaimer and avoid deterministic promises, luck-changing claims, or medical efficacy claims.
- Core consumer flow: identify bracelet without login, land on the trusted-credential homepage, then optionally enter the simulated divination Agent, inspect certificate and traceability, or access care guidance and agarwood knowledge. NFC identifies the bracelet and opens the homepage; it does not directly start a reading.
- Fidelity feedback (2026-08-20): when a visual direction is selected, the homepage must reproduce its composition and hierarchy directly. Do not reinterpret it with a new hero headline, card system, alternate palette, or unrelated content grid.
- Agent visual feedback (2026-08-21): avoid generic AI chat tropes such as round avatars, online dots, left/right bubbles, repeated prompt cards, and nested result cards. Use the approved `沉香问帖` editorial structure instead: current-bracelet identity band, question slip, Agent annotations, and a single un-nested reading folio. Preserve readable mobile type, 44px touch targets, safe-area composer behavior, and keep the newest Agent response visible without clipping its beginning.
- Homepage hierarchy feedback (2026-08-21): changing only the Agent screen is not a sufficient redesign. The approved homepage direction is `问帖主场`: keep the verified bracelet as the first context, make `为本串问一帖` the single dominant above-fold action, retain `AI传统文化解读` as its business label, and reduce certificate, care, knowledge, and advisor functions to archive-style secondary links instead of equal-weight cards or grids.
- Mini-program redesign decision (2026-08-24): the product will become a WeChat Mini Program and the selected visual direction is `随身香事`. Replace the archive-concept presentation with a daily-use product structure: `当前手串 → 今天想问什么 → 最近问帖 → 首页/问帖/我的`. Use a realistic Mini Program navigation bar and capsule safe area, system UI typography with limited Songti accents, moderate shared radii, ordinary list rows, and a focused conversation record. Remove English archive labels, decorative folio borders, vertical annotations, repeated cards, AI avatars/online dots, gradients, glows, and generic chat bubbles. The React prototype simulates the Mini Program UI; native WXML/WXSS migration is a separate future scope.
- Root-tab responsibility decision (2026-08-24): homepage is for immediate use (identified bracelet, reading CTA/history, traceability/care/knowledge, and a secondary Guangken agarwood story Banner). “我的” is for records and management only (reading records, bracelet management, NFC explanation, interpretation explanation). Do not repeat the full bracelet card, certificate entry, or care entry in “我的”.
- Mini Program authorization requirement (2026-08-24): “我的” must expose a WeChat authorization login entry. In the React requirements prototype, simulate the before/after authorization state locally and label it as a prototype experience; native Mini Program authorization and backend account binding remain implementation scope outside this prototype.
- Reading authorization gate (2026-08-24): every reading entry (home CTA, root 问帖 tab, and profile reading records) requires the shared WeChat authorization state. If unauthorized, present a phone-scoped bottom sheet; authorizing must continue directly into the intended reading flow. Do not duplicate independent authorization state per page.
- Homepage redesign selection (2026-08-24): the approved direction is `手串即首页`. Merge the top image-ad slot, Guangken provenance, verified bracelet identity, and bracelet metadata into one product-led hero using the real bracelet image; remove the separate repeated Banner and current-bracelet block. Place `为这串沉香，问一件挂心事` and the only solid primary CTA directly below the hero. Keep provenance, recent reading, and services as lightweight divided rows. The hero must use a solid caption band rather than a gradient overlay, and the reading CTA must remain fully above the fixed tabs on both iPhone and Pixel.
- Homepage full-bleed refinement (2026-08-24): the product hero is a top full-bleed Banner that begins at the device screen edge and extends behind the status-bar/battery area, with no outer margin and zero border radius. The homepage root has no visible “我的沉香” title; retain only the native Mini Program capsule over the image. The homepage must fit in one non-scrolling screen above the fixed tabs on both iPhone and Pixel. Preserve certificate, care, and knowledge as a single compact three-way text action strip instead of three stacked service rows.
- Homepage asset policy (2026-08-24): verification, AI, provenance/location, recent-reading, and bottom-tab icons must use native controls with the shared icon library, never raster slices. The reading CTA remains a semantic native button with live text and icon; its supplied text-free raster may be used only as the decorative frame. Use the supplied transparent 3D illustrations for certificate, bracelet care, and agarwood knowledge.
- Home responsibility update (2026-08-24): 首页不展示最近问帖或独立农垦故事卡；历史记录统一归入“我的”。首页三项服务的整格均为可点击热区，分别进入证书、养护和知识二级页；农垦故事入口迁入“沉香知识”，故事页讲产区与手作，证书页保留本串时间线。
- Profile redesign selection (2026-08-24): “我的”采用 C「个人香事档案」。微信授权的未登录/已登录状态必须在同一张米白身份卡内切换；“问帖记录”和“我的手串”合并为一组深绿双入口，作为首屏主任务；“芯片识别说明”和“关于传统文化解读”使用无外层卡片的分割线清单。删除独立大标题、统计区、重复分组卡片和底部脚注，保持 iPhone 与 Pixel 一屏展示。
- Standalone AI fortune chat update (2026-08-24): the latest approved `AI问事` direction overrides the earlier bracelet-linked editorial transcript guidance. Remove the current-bracelet identity block from the reading screen and present the experience as a native conversational Agent: Agent messages on the left, user messages on the right, three compact starter topics, proactive follow-up questions, plain-language `判断 / 提醒 / 建议` answers, a persistent keyboard-aware composer, and the exact disclaimer `内容由 AI 生成，仅供娱乐参考`. Do not show bracelet metadata, hexagram/result cards, AI avatars, online indicators, or the root tab bar inside the pushed chat screen.
- Global UI P0/P1 remediation decision (2026-08-24): this pass does not authorize another homepage redesign. `问帖记录` must open a real history list and read-only conversation detail before `继续追问`; it must never route directly to a new reading. For the current single-bracelet prototype, use the honest label `本串证书` when the destination is the certificate page. Profile help rows are actionable routes, the reading disclaimer stays fixed with the composer, authorization resumes the user's original intent, and care/knowledge pages must contain complete editorial content rather than half-empty placeholders.

## Product Consolidation Decision (2026-08-24)

- The approved MVP position is `沉香数字身份 + AI传统文化解读 Agent`. The AI experience may use restrained traditional-culture language, but it is not a deterministic fortune engine and must not promise predictions, luck-changing, medical efficacy, or professional medical/legal/financial conclusions.
- The authoritative product behavior source is `docs/codex/prd/2026-08-24-agarwood-digital-identity-ai-traditional-culture-agent-mvp-v2.md`, currently document version 3.0. Earlier PRDs, older specs, and implementation plans remain historical visual/technical references only; where they conflict, document version 3.0 and later explicit user instructions win.
- The AI Agent must ask for missing context before answering, then provide plain-language `判断 / 提醒 / 建议`. Keep the AI experience independent from bracelet metadata and do not imply that bracelet material, origin, or identity affects fate.
- Every AI experience must include the exact disclaimer `内容由 AI 生成，仅供娱乐参考`. The latest composer decision supersedes the prior fixed-disclaimer rule: the chat bottom keeps only the keyboard-aware input shell, the microphone stays inside it, no external send button is shown, Enter sends text, and the disclaimer belongs in the conversation content.
- Native Mini Program login uses `wx.login` plus a server session for basic identity. Avatar/nickname authorization is optional and purpose-specific, not a prerequisite for certificate viewing or text chat. Microphone permission is requested only when voice is first used, and authorization must resume the user's original intent.
- Homepage responsibilities remain: verified bracelet hero, one primary AI entry, and certificate/care/knowledge services. Reading history belongs in `我的`; farm-story content belongs under `沉香知识`. `我的` is a personal archive for session state, reading records, the current certificate, and help—not a duplicate homepage.
- Production release is gated by WeChat category/content pre-review, privacy/legal review, traceability-issuer validation, and AI safety verification. Do not treat the React requirements prototype as production approval.
- Prototype review-document placement decision (2026-08-24): product requirements and review documentation belong outside the phone model. Expose them through browser-level prototype review chrome, not through Mini Program routes, tabs, profile/help rows, or phone-scoped overlays. The selected presentation is a right-side drawer that keeps the phone model intact and operable.
- Contextual review-document decision (2026-08-24): the browser-level product document drawer defaults to the active phone page's `DOC-P-xx` delivery notes and provides an explicit `完整 PRD` mode. Determine the active page from stable app-owned page IDs, not translated titles. A visible authorization sheet maps to P-02 and overrides its underlying page; the complete PRD and page guides share the same authoritative Markdown source.

## Customer Supplement Decision (2026-08-28)

- The customer-selected homepage visual source is the supplied Guangken reference composition with the logo, Eastern garden background, illustrated girl holding a bracelet, warm paper cards, deep green actions, and native Mini Program capsule. This latest explicit visual choice overrides the prior real-bracelet full-bleed hero direction for the homepage.
- The homepage is now a vertically scrollable content page rather than a mandatory one-screen composition. Its order is: brand hero, revised AI incense-culture entry, Guangken selection showcase, personal collection carousel, four service entries, industrial-park trust block, agarwood knowledge rows, and company copyright.
- Root tabs are now `首页 / 商城 / 问帖 / 我的`. The mall is not implemented in this phase: its tab and all mall-oriented showcase entries only surface a lightweight `即将上线` message and must not navigate to an empty or fake commerce page.
- This phase must implement every other item in the customer supplement: bracelet/tree collection switching, collection detail, downloadable prototype PDFs for the bracelet and adopted tree, the fourth tree-adoption service entry, a complete adoption archive with growth timeline, an industrial-park image carousel and simulated video state, knowledge destinations, and preserved existing authorization/AI/history/certificate/help flows.
- The authoritative design for this supplement is `docs/superpowers/specs/2026-08-28-customer-feedback-home-expansion-design.md`.
